<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kjk.nyam.mapper.RestaurantIdMapper">
	<select id="selectREIdList" resultType="reId">
		select * from restaurant_id
	</select>	
</mapper>